name: template
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.5 <3.0.0"

dependencies:
  device_preview: ^1.1.0 # ToDo: Can be removed for prod
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.2
  flutter_localizations:
    sdk: flutter
  fpdart: ^0.5.0
  freezed: ^2.3.2
  freezed_annotation: ^2.2.0
  go_router: ^6.5.2

dev_dependencies:
  analysis_options: any
  bloc_test: ^9.0.0
  build_runner: ^2.3.3
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  lint: ^2.0.1
  test: ^1.16.0

flutter:
  uses-material-design: true
  assets:
    - lang/
#    - assets/images/
